[build.env]
passthrough = ["OPENSSL_STATIC", "OPENSSL_VENDOR"]

[target.x86_64-unknown-linux-musl]
pre-build = ["apt-get update && apt-get install -y cmake perl"]

[target.aarch64-unknown-linux-musl]
pre-build = ["apt-get update && apt-get install -y cmake perl"]
