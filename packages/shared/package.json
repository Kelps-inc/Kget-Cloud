{
  "name": "@kget-cloud/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.lint.json",
    "test": "bun test --pass-with-no-tests"
  },
  "devDependencies": {
    "typescript": "5.4.5"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
